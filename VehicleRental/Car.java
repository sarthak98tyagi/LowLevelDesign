package VehicleRental;

public class Car extends Vehicle{


    public Car(String vehicleNumber, String brand, String modelName, VehicleType vehicleType, VehicleStatus vehicleStatus, EngineType engineType) {
        super(vehicleNumber, brand, modelName, vehicleType, vehicleStatus, engineType);
    }
}
